# 更换 DeepSeek API Key 测试方案

## 目标
验证更换 API Key 后，应用程序的数据（学习进度、笔记、缓存）是否按预期更新，确保新 Key 能正常工作且不会导致数据混乱。

## 背景
应用程序在 localStorage 中存储以下类型的数据：
1. **API Key 本身**：`deepseek_api_key`
2. **学习笔记**：`study_notes` 和 `study_notes_owner`（与 API Key 绑定）
3. **词汇练习进度**：`vocab_drill_session_*`、`vocab_drill_review_counts_*`、`vocab_drill_cover_states_*`（与课程 ID 绑定，与 API Key 无关）
4. **AI 请求缓存**：内存缓存（页面刷新即丢失）
5. **其他设置**：如主题、语言等（如果有）

当用户更换 API Key 时，预期行为如下：
- 笔记应清空（因为笔记属于原用户）
- 词汇练习进度应保留（因为进度属于课程，与用户无关）
- AI 功能应使用新 Key 正常工作
- 不应出现旧 Key 残留导致请求失败

## 测试前准备
1. **备份当前数据**（已完成）
   - 代码备份：`src_backup_20251210_with_audio/`
   - 数据备份：可通过浏览器开发者工具导出 localStorage（可选）

2. **准备两个有效的 DeepSeek API Key**
   - Key A（当前正在使用的 Key）
   - Key B（用于测试的新 Key）

## 测试步骤

### 步骤 1：使用当前 Key 验证初始状态
1. 在 Chrome 中打开应用程序（正常窗口）。
2. 确保已设置 Key A（可通过“API Key设置”面板查看）。
3. 创建一些测试数据：
   - 在“贴纸背单词”中点击几个单词，增加背诵次数。
   - 在“学习笔记”侧边栏中输入一些文字。
   - 进入“语法精讲”生成一次 AI 讲解（确保 AI 功能正常）。
4. 记录当前进度（例如，某个单词的背诵次数、笔记内容）。

### 步骤 2：更换 API Key
1. 在“API Key设置”面板中点击“清除”按钮，移除 Key A。
2. 输入 Key B，点击“保存”。
3. 观察界面变化：
   - 笔记侧边栏应自动清空（因为 `study_notes_owner` 已变更）。
   - 词汇表格和背诵次数应保持不变。
   - 其他 UI 元素无异常。

### 步骤 3：验证数据隔离
1. 刷新页面（F5）。
2. 检查笔记是否仍为空（预期为空）。
3. 检查词汇进度是否保留（预期保留）。
4. 检查 API Key 设置面板是否显示 Key B（预期显示）。

### 步骤 4：测试 AI 功能
1. 进入“语法精讲”板块，选择任一语法点，点击“AI深度讲解”。
2. 等待生成结果，确认内容正常（无错误提示）。
3. 进入“翻译训练”，生成翻译题目并提交，确认评分功能正常。
4. 观察浏览器控制台（F12 → Console）是否有 API 错误。

### 步骤 5：极端情况测试
1. **快速切换 Key**：在 Key A 和 Key B 之间来回切换多次，检查笔记是否每次切换都被清空（预期是）。
2. **无 Key 状态**：清除 Key 后，尝试使用 AI 功能，应提示“请先设置 API Key”。
3. **无效 Key**：输入一个无效 Key（格式正确但权限无效），尝试生成内容，应收到 API 错误提示（状态码 401）。

### 步骤 6：清理与恢复
1. 测试结束后，可根据需要恢复为原来的 Key A。
2. 如果测试过程中数据出现意外，可从备份恢复 localStorage（或直接清除 localStorage 重新开始）。

## 预期结果
- ✅ 笔记随 Key 更换而清空，避免用户数据混淆。
- ✅ 词汇进度、贴纸状态、背诵次数等学习数据不受 Key 更换影响。
- ✅ AI 功能使用新 Key 正常工作，无残留旧 Key 请求。
- ✅ 页面刷新后状态保持一致。
- ✅ 错误处理友好，无效 Key 给出明确提示。

## 故障排查
如果测试中发现异常，请检查以下方面：

1. **笔记未清空**
   - 检查 `StudyNotes.jsx` 中 `useEffect` 依赖项 `apiKey` 是否正确传递。
   - 检查 localStorage 中 `study_notes_owner` 是否随 Key 更新。

2. **词汇进度丢失**
   - 确认 localStorage 中 `vocab_drill_*` 键值是否在 Key 更换后仍然存在。
   - 确认组件是否因 API Key 变化而意外重新初始化。

3. **AI 请求仍使用旧 Key**
   - 检查 `deepseekAPI.js` 中 `getApiKey()` 是否每次都从 localStorage 读取最新值。
   - 检查是否有任何缓存机制（如 `requestCache.js`）错误地缓存了旧 Key 的请求（目前缓存仅为内存缓存，刷新即失效）。

4. **控制台错误**
   - 查看 Network 面板，确认请求头中的 Authorization 字段是否为新 Key。
   - 检查是否有跨域（CORS）问题。

## 自动化测试建议（可选）
如需长期维护，可编写简单的 Cypress 或 Playwright 测试脚本，模拟上述步骤。示例脚本可放在 `cypress/e2e/api-key-switch.spec.js` 中。

## 结论
通过以上测试，您可以确信更换 API Key 不会破坏现有学习进度，同时保证了用户数据的隔离性。如果所有测试通过，即可放心交付给最终用户。

---
*最后更新：2025-12-10*